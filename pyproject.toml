[project]
name = "grievance-sih"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend", "ml"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
