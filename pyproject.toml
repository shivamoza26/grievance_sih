[project]
name = "grievance-portal"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = ["backend", "ml"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
